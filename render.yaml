services:
  - type: static_site
    name: project-management-frontend
    env: static
    branch: main
    plan: free
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html